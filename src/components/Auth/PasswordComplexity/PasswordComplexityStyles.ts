import { makeStyles } from '@material-ui/styles';

const usePasswordComplexityStyles = makeStyles(() => ({
    root: {

    },
    password: {
        height: '5px',
        transition: 'all 300ms ease-in-out'
    }
}));

export default usePasswordComplexityStyles;